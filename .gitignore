# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies (any folder named node_modules — not only repo root)
node_modules/
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js (build output in any app folder, e.g. frontend/.next)
.next/
out/
.turbo/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# local Lambda secrets / large deploy artifacts
infra/agristack-lambda/.proxy-secret.local
infra/agristack-lambda/function.zip

# --- Conda / local Python environments (do not commit) ---
.conda/
env/
venv/
.venv/
ENV/
env.bak/
.python-version
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
*.so
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/

# --- Google Earth Engine / GCP service account (secrets) ---
api/gee_service_account.json
**/gee_service_account.json

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# LGD download / unpack scratch (village CSV is ~66MB)
frontend/scripts/lgd_tmp/
