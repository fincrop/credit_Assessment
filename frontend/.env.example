# Optional: default API base when developing (Vite injects only VITE_* vars)
# VITE_API_BASE_URL=http://127.0.0.1:8000

# Optional: pre-fill the API key field in the UI (still visible in browser — prefer server-side BFF in production)
# VITE_API_KEY=
